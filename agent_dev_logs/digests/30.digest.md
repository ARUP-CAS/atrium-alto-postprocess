# 📓 Digest — Issue #30: Algorithm change for 2010+ years documents

> *Opened as a narrow pipeline-alignment check, then materially addressed by PR #32 (“Refs #30”), merged into `test` on 2026-07-26. The follow-up review cycle that followed surfaced a real regex bug and an architecture question about how the categoriser’s structural rules should fire. Both are now resolved in `test`. A later corpus-scale validation of `50472fb` confirmed the guard restoration is clean at full MTX scale, the blind test remains at the best measured state, and the spaced metre candidate is clean enough to remain a dormant design choice rather than a defect.*
>
> *Clarification from the PR author, unchanged: the algorithm change targets the corpus as a whole; 2010+ was the highest-priority slice for Dana, which is why the issue was introduced that way.*
>
> *Thread: 2026-07-23 (opened) → 2026-07-26 (PR #32 merged, `f6adc23`) → 2026-07-26/27 (follow-up commits `b50c1f3` → `6486c72` → `641f936`) → 2026-07-27 (4-item action plan posted; issue still open) → 2026-07-28 (full-corpus validation of `50472fb`; issue ready to close) · K4TEL, david-spacil, with repeated cross-model review/measurement passes.*

**Labels**: `development`, `enhancement`, `integration` · **Milestone**: Q3 [WP8/WP7]
**Status**: ✅ `Ready to close` from the review side. The remaining code path is validated at corpus scale; the only thing left is the issue close-out itself.

## 🎯 Goal

The issue body is narrow: *“Proposed at the meeting - to be checked with tools/recategorize … make sure the new algo is aligned with the pipeline.”* The PR-author clarification remains important: this was always meant to cover the corpus as a whole, with the 2010+ subset treated as the most important slice for the original request. The core task is verification of the categorisation pipeline, not a standalone feature request.

## 🔧 What changed — PR #32 (merged 2026-07-26, `f6adc23`)

PR #32 merged seven commits into `test`, concentrated in `text_util.py` (+309/−5). Six independently reviewable logic changes, plus a CodeQL fix:

* `2 → z` OCR repair no longer fires inside numbers.
* Neutral/reference tokens (dates, cites, initials, sigla, dotted abbreviations, units) are excluded from the valid-word ratio.
* Short lines (1–2 words) are routed through a structural gate instead of language confidence.
* A character-level damage detector caps damaged lines at `Noisy`.
* A clean-reference floor can lift some `Trash` lines to `Noisy`, never the reverse.
* Extra fragment/bigram/case-mix/solitary-letter rules push obvious garbage to `Trash`.

Measured with `tools/recategorize_from_csv.py`, its own stated instrument: 61.3% → 79.0% on a 300-line blind sample, with a real recall/precision tradeoff on the hardest “needs attention” band (recall 72.2% → 46.3%, precision 30.7% → 50.0%).

## 🔁 What changed since — the follow-up chain (`b50c1f3` → `6486c72` → `641f936`)

PR #32 shipped with several tests still red. What followed was a same-day, then next-day, cross-model review cycle that treated the merge as a checkpoint, not a finish line:

* **Root cause found first.** Several `test_recategorize_parity` failures traced to one thing: `rule_lowppl_clear` short-circuiting straight to `Clear` before `is_forgiven_headline`/`check_rescues()` ever got a chance to run, on a 9-word em-dash-separated title line. The bug predated the WIP commit.
* **`b50c1f3`** fixed the immediate parity failures (495 passed) and shifted real category volume on the sample docs (`Clear` +130, `Noisy` −101, `Trash` −29 across 251 changed lines).
* **A same-day logic audit** of that commit flagged two of its changes as substantive judgment calls rather than bug fixes, not just plumbing:

  1. the six pre-existing structural override rules (rotation/wqx, vowel-less, ledger fragmentation, mid-uppercase, bigram runs, fragment tokens) had been pulled out of `determine_category()` and reimplemented as cumulative `qs -= 0.35` quality-score penalties — reversing the pure threshold-routing architecture the categoriser had been moved to earlier;
  2. a new blanket `" — " + digit` substring match was forcing any such line straight to `Noisy` — directly undercutting PR #32’s own argument that its flagship em-dash fixture had simply been mislabeled, not that em-dashes-with-digits are inherently bad.

  * Cross-model consensus converged fast on both: restore the six rules as **named, explicit gates**, not penalties; and either narrow the em-dash matcher to the actual failure mode or just fix the fixture.
* **`6486c72`** partially answered this — restored the character-level damage backstop (`if damaged: return "Noisy"`) and tightened `is_structured_line`/`_looks_like_measurement` to reject bare single-letter units. Blind-sample measurement: 81.0% (243/300), statistically indistinguishable from the other calibrated commits (McNemar n.s.) — but the same review found two loose predicates still letting probe garbage (`"3 m"`, `"cuxoaid v. 12"`) through as falsely “structured.”
* **`641f936`** restored the six rules as explicit gates — satisfying the architecture request — and tightened the measurement predicates further. It also introduced two regressions of its own:

  1. **A real regex bug.** The tightened `full_measurement_keywords`/`descriptor_keywords` patterns ended several alternatives in a literal `.` (`rozm\.`, `pr\.`, `hl\.`) inside a single alternation wrapped in one trailing `\b`. A boundary cannot exist right after a non-word character (`.`) when the next character is also non-word (a space) — so `"pr. okraje - 145"`, `"rozm. 12"`, and `"pr. dna - 7"` silently lost measurement protection whenever OCR left a space after the dot, while glued forms (`"pr.okraje"`) kept working. Restricting units to multi-character-only also cost roughly 3,282 lines carrying a bare metre — the single most common unit in the corpus by a wide margin.
  2. `is_forgiven_headline`’s docstring and all inline step-comments were deleted outright in the same edit — not shortened, removed — with no note explaining why. The underlying logic was untouched.

  * Net corpus effect (9.1M lines): 11,955 moved, 99.95% of them demotions (`Clear→Noisy` 7,460, `Clear→Trash` 3,093, `Noisy→Trash` 1,396). Blind-sample accuracy did not move (still 243/300) — the corpus-scale effect was invisible at n=300.
  * One incidental finding, explicitly flagged as neither a regression in intent nor something to fix in the same pass: ~237 copies of `"Plocha: 3; sonda: XIV."`-style archaeological headers lost a protection that had only ever existed by accident. Logged as “needs somewhere else to land,” not actioned.

## 🩹 The 4-item action plan and where it stands

Posted 2026-07-27 (“By Gemini and GPT”) in direct response to the `641f936` measurement above:

| # | Item                                         | Status                                                                                                                                                                                                                        |
|---|----------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | Fix the regex boundary; restore the `m` unit | ✅ Merged to `test`, independently re-verified — dotted entries split out of the shared trailing `\b` alternation; `m` restored only for digit-glued forms (`"0,46m"`), spaced forms (`"3 m"`) still correctly rejected        |
| 2 | Restore the six rules as explicit gates      | ✅ Already satisfied by `641f936` itself — confirmed live, no `qs -=` penalty pattern remains active anywhere in `text_util.py`                                                                                                |
| 3 | Replace/remove the em-dash + digit patch     | ✅ Already resolved — no such pattern exists anywhere in current `text_util.py`; the `CTX000000001` L1 fixture is correctly baselined `Clear`                                                                                  |
| 4 | Restore `is_forgiven_headline` documentation | ✅ Merged to `test`, independently re-verified — the original docstring and comments were recovered from `6486c72` and confirmed byte-identical in logic to current HEAD, so this is a verbatim restore, not a reconstruction. |

Two regression tests were also added, isolating the boundary bug specifically. Full suite on `test` HEAD, independently re-run from a fresh checkout: 510 passed / 4 skipped / 4 deselected.

## ✅ Full-corpus validation of `50472fb`

A later full-corpus pass confirmed the patch set is stable at scale:

* **Guard restoration:** `eee7c89` → `50472fb` changed 2,344 lines, with 2,344 lifted / 0 demoted.
* **Blind test:** unchanged at 243/300, with `b=0, c=0`.
* **Metre-spacing probe:** the corpus evidence is clean, and wiring the candidate into `has_unit` still produces no demotions.

The key result is not just that the metre candidate is large; it is that it is not destructive. The affected lines are mostly depth records (`0,2-0,4 m`, `0,4-0,6 m`, `0-0,2 m`, `0,06 m`, `0,15 m`), and the changes remain upward-only. The one dormant test that asserts the probe is not yet wired stays valid by design.

## 🧭 What the current `test` branch docs say

Unchanged from before, and still accurate post-`50472fb`:

* `forgiven` is computed after the earlier gates, so it can only rescue lines that would otherwise fall to `Trash` late in the pipeline.
* The structural rules for rotated/wqx-heavy, vowelless, fragmented, and mid-uppercase lines sit alongside `check_rescues()`, which can still rescue some lines from `Trash` to `Noisy`.
* The main score-band routing is still threshold-based, with `lm_confident_czech` and `rule_lowppl_clear` acting as important exceptions.
* `categorize_line()` clamps the stored `quality_score` to the band implied by the final label.
* `apply_document_postprocessing()` is still part of the production path and is confirmed reused, byte-for-byte, by `tools/recategorize_from_csv.py`, `classify_TEXT.py`, `test_smoke.py`, and `test_rotation_regression.py` — all four import directly from `text_util.py`, no parallel implementation exists anywhere.

## ⚠️ What still looks suspicious

The scope-drift concern from the original digest is now smaller, not larger: the architecture question was settled in favor of explicit gates, the regex defect was fixed, and the corpus-scale validation came back clean. What remains is mainly a product-choice boundary, not a correctness issue.

The only remaining decision-like item is the dormant metre-spacing candidate: it is now corpus-clean enough to keep as a design choice, but it still does not need to be wired in unless the project wants that extra coverage.

## ⏳ Open

* Close issue #30 now that the full-corpus validation of `50472fb` is clean.
* Decide separately whether the dormant metre-spacing candidate should be promoted in a future change set.
* Keep `_DMG_SYMBOLS` unchanged; the measurement still supports the review’s conclusion that it is not worth a local rule patch.

---

*Digest refreshed from the issue thread through the 2026-07-28 full-corpus validation comment, plus the current `test` branch documentation and the earlier review chain.*
