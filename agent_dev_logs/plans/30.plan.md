# 🧭 Plan — Issue #30: Algorithm change for 2010+ years documents

> Issue #30 · Labels: `development`, `enhancement`, `integration` · **Status**: ready to close after full-corpus validation. PR #32 and the follow-up review chain resolved the original alignment problem, the regex boundary bug, and the architecture question. The remaining metre-spacing candidate is a dormant design choice, not a blocker.

## Context

This issue is a pipeline-alignment check, using `tools/recategorize_from_csv.py` as the verification path. The PR-author clarification still matters: 2010+ was the highest-priority slice for Dana, but the change was always intended to be corpus-wide.

The review cycle after PR #32 was useful because it separated three different things that had become entangled:

* a genuine parity bug in `rule_lowppl_clear`,
* a real architecture question about gates vs. penalties,
* and a regex boundary bug in the measurement predicates.

All three were fixed or settled on `test`.

## Status snapshot

| Item                                                  | State                               | Note                                                                                                                                                           |
|-------------------------------------------------------|-------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Issue #30 ask                                         | ✅ addressed in principle            | pipeline alignment verification via `tools/recategorize_from_csv.py`                                                                                           |
| Scope reading                                         | ✅ clarified                         | corpus-wide change, 2010+ as highest-priority subset                                                                                                           |
| PR #32                                                | ✅ merged to `test`                  | `f6adc23`, 2026-07-26, seven commits                                                                                                                           |
| Follow-up commit chain                                | ✅ merged to `test`                  | `b50c1f3` → `6486c72` → `641f936`; responded to the same-day review cycle and resolved the gates-vs-penalties question plus the regex/docstring regressions    |
| Parity-test root cause                                | ✅ found and fixed                   | `rule_lowppl_clear` short-circuit before `is_forgiven_headline`/`check_rescues()`; fixed in `b50c1f3`                                                          |
| Six-rule architecture (gates vs. penalties)           | ✅ resolved                          | restored as named, explicit gates in `641f936`; no penalty-cascade implementation remains                                                                      |
| Em-dash + digit patch                                 | ✅ resolved                          | removed; no such pattern exists in current `text_util.py`                                                                                                      |
| Regex boundary fix + `m` unit (action-plan item 1)    | ✅ merged, independently re-verified | on `test` HEAD (`6df88f3`); byte-diffed against the reviewed patch (identical) and re-run from scratch                                                         |
| `is_forgiven_headline` docstring (action-plan item 4) | ✅ merged, independently re-verified | on `test` HEAD (`6df88f3`); matches the restored wording line-for-line                                                                                         |
| Regression coverage for the boundary bug              | ✅ added                             | two tests in `tests/test_categorization_routes.py`                                                                                                             |
| Production/offline-rescorer parity                    | ✅ confirmed                         | `classify_TEXT.py`, `tools/recategorize_from_csv.py`, `test_smoke.py`, `test_rotation_regression.py` all import scoring functions directly from `text_util.py` |
| Full-corpus validation                                | ✅ confirmed                         | `50472fb` passed the full MTX corpus check with upward-only movement and no blind-test regression                                                              |
| `_DMG_SYMBOLS` false positives                        | ✅ left unchanged                    | review conclusion stands: this is not worth a character-list patch                                                                                             |
| `sonda: XIV.` / metre-spacing candidate               | 🔶 dormant candidate                | probe stays unit-tested but unwired; corpus evidence is clean, but adoption remains a design choice, not an issue blocker                                      |
| Final sign-off                                        | ✅ ready to close                    | no additional correctness fix remains                                                                                                                          |

## Review checklist

1. Re-run the offline replay path on the current `test` branch and confirm the replayed labels still match production expectations. ✅ Done at fixture/unit-test level and then at full-corpus scale.
2. Separate the changes into two buckets — alignment fixes required by #30, vs. broader categorisation-policy changes. ✅ Done: the remaining open work is no longer a correctness issue.
3. Audit the short-line gate, clean-reference floor, and fragment rules for unintended effects on archival short forms, catalogue lines, and measurement notation. ✅ The corpus validation did not surface a blocker.
4. Confirm whether the late-stage penalty gates are intended to be permanent policy or are still experimental. ✅ Resolved — they are explicit named gates.
5. Check whether the docs’ claim that the offline re-scorer and production postprocessing are reused byte-for-byte still holds after the latest branch state. ✅ Confirmed directly.
6. Decide whether #30 can be closed as “implemented and verified.” ✅ Yes, based on the latest full-corpus validation.

## Deliverable shape

The final comment on the issue should say:

* what the issue asked for,
* what PR #32 changed, and what its own follow-up commits changed on top of that,
* what the full-corpus validation of `50472fb` confirmed,
* and which things remain intentionally out of scope, especially `_DMG_SYMBOLS` and the dormant metre-spacing candidate.

That keeps the thread focused on verification, while making it clear that the issue is ready to close.

## Open items for follow-up

* Close the issue on GitHub now that full-corpus validation is clean.
* Decide later, in a separate change set, whether the dormant metre-spacing candidate should be promoted.
* Keep `_DMG_SYMBOLS` unchanged unless a future detector-quality pass shows a real need for a new rule.

---

*Plan refreshed from the live issue through the 2026-07-28 full-corpus validation comment, the follow-up commit chain, and the current `test` branch documentation.*
