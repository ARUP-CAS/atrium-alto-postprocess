# Strategy — Issue #30: Algorithm change for 2010+ years documents

> Issue [#30](https://github.com/ufal/atrium-alto-postprocess/issues/30) · Labels: `development`,
> `enhancement`, `integration` · **Status: 🔍 tool/pipeline alignment fix landed on `test`
> (`b50c1f3`, 2026-07-26) — not yet reviewed, not yet merged to `main`**

## Context
#30 has no scope of its own beyond one line: verify, using `tools/recategorize_from_csv.py`,
that whatever new categorization algorithm comes out of the ongoing #3 calibration thread stays
aligned with the production pipeline (`classify_TEXT.py`). It is effectively a **verification
gate** on #3's output, not an independent feature.

That output arrived as **PR #32**, "calibration of the per-line categorisation logic"
(david-spacil, code by Claude Opus per its provenance note), merged into `test` as `f6adc23` on
2026-07-26. The PR's own Testing section explicitly refs #30 — every number it reports was
produced by `tools/recategorize_from_csv.py` calling the real `compute_quality_score` /
`categorize_line` / `apply_document_postprocessing`, not a parallel re-implementation. On paper,
that already satisfies #30.

In practice, three more commits landed on `test` the same day (`228ac9a`, `2ee70bd`, `b50c1f3`,
all K4TEL — matches the PR comment *"will look into it tonight/tomorrow and maybe fix some of
the errors from tests"*), which both fixed a genuine alignment gap in the tool **and** reworked
the shipped categorization logic well beyond what the 8 known-failing tests required. This plan
covers verifying the merge, then itemizing exactly what changed after it.

## Status snapshot
| Item                                                           | State          | Note                                                                                               |
|----------------------------------------------------------------|----------------|----------------------------------------------------------------------------------------------------|
| PR #32 merge (`f6adc23`)                                       | ✅ **DONE**     | 6 substantive commits + 1 CodeQL fix; all present and unmodified in current `text_util.py`         |
| `tools/recategorize_from_csv.py` used as sole measurement tool | ✅ **DONE**     | Every PR #32 metric traced to it; directly satisfies #30's literal ask                             |
| Tool/pipeline alignment fix (#30's actual content)             | ✅ **DONE**     | `_rescore_row()` now uses `original_text` + two-tier trust lang-score, matching `classify_TEXT.py` |
| 8 known-failing tests from PR #32                              | ✅ **RESOLVED** | All pass on current HEAD — but via logic changes beyond what the PR proposed (see diff below)      |
| Full test suite                                                | ✅ **GREEN**    | `pytest -m "not slow"` → 489 passed, 4 skipped, 0 failed (re-verified against a fresh clone)       |
| Review of the post-merge rework                                | ⏳ **OPEN**     | Not discussed anywhere in the issue or PR thread yet                                               |
| Dead code cleanup (`text_util.py:1383–1802`)                   | ⏳ **OPEN**     | ~450 lines of commented-out superseded `determine_category`/`categorize_line` versions             |
| `test` → `main` merge                                          | ⏳ **OPEN**     | No release tag for this pass, unlike #3's `v0.20.1`                                                |

---

## Categorization-logic diff: PR #32 (`f6adc23`) → current `test` HEAD (`b50c1f3`)

All line numbers below are from the current `text_util.py` (2136 lines).

| # | Change                                   | Where                                                               | Mechanism                                                                                                                                                                                                                                                                                 |
|---|------------------------------------------|---------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | `rule_short_garbage` reordered + widened | `:1198` (was evaluated later, near old rule 6)                      | Now fires *before* the 5a short-line gate; condition widened from `lang_score <= LANG_SCORE_REMAP` to `(lang_score <= LANG_SCORE_REMAP OR rot_ratio >= SUSPICIOUS_ROT_RATIO)`                                                                                                             |
| 2 | Cumulative penalty cascade reinstated    | `:1314–1349` (`categorize_line`); removed from `determine_category` | Six rules (`penalty_wqx_rot`, `penalty_vowelless`, `penalty_ledger_fragmentation`, `penalty_mid_uppercase`, `rule_bigram_run`, `rule_fragment_tokens`) now do `qs = max(0.0, qs - 0.35)` *before* `determine_category` runs, instead of independently forcing Trash via `check_rescues()` |
| 3 | Unfilterable symbol/number override      | `:1240`                                                             | Inside the 5a Clear branch: zero alphabetic characters + not forgiven/reference/upright-Czech → Trash                                                                                                                                                                                     |
| 4 | Em-dash + digit override                 | `:1257` (inside `rule_lowppl_clear`)                                | `" — " in text_source and any digit` → forced `Noisy`, never `Clear`                                                                                                                                                                                                                      |
| 5 | `recategorize_from_csv.py` alignment fix | `tools/recategorize_from_csv.py:_rescore_row`                       | Reads `original_text` (not pre-filtered `text_content`) for density/vowel signals; lang_score via two-tier trust matching `classify_TEXT.py`                                                                                                                                              |
| 6 | Dead code left in file                   | `:1383–1802`                                                        | Three full commented-out historical versions of `determine_category`/`categorize_line`                                                                                                                                                                                                    |
| 7 | Fixture re-baseline                      | `data_samples/DOC_LINE_CATEG/CTX00000000{1,2,3}.csv`                | `quality_score` values updated; **zero `categ` labels changed** — the re-baseline is a pure number refresh, not a relabeling                                                                                                                                                              |

### 1 — `rule_short_garbage`: reorder, not a new rule
This rule predates PR #32 (from the earlier #3/v8 calibration). At merge time (`f6adc23`) it sat
*after* the LM override, late enough that the PR's new 5a short-line gate intercepted short
gibberish first and routed it to `Clear` — which is exactly what the PR's "8 tests fail by
design" section describes (`olie`, `oueussd` → `Clear`). Moving it ahead of 5a, and adding the
`rot_ratio` alternative, restores the old `Trash` verdict for these without touching the 5a gate
itself. Low-risk, well-targeted fix.

### 2 — Penalty cascade: a real architecture reversal
Before PR #32 (v0.15.4, 2026-06-15): *"pure threshold routing on `quality_score` drives the
category … + 3 structural overrides replaced the old ~150-line penalty cascade"* (per
`agent_dev_logs/DEVLOG.md`). PR #32 kept that design and added its four new pieces
(`_is_neutral_token`, short-line gate, `count_damaged_tokens`, `is_clean_reference`) as more
independent overrides in the same style.

The follow-up commits pulled the six pre-existing override rules out of `determine_category`
entirely and reimplemented them as scalar `qs` penalties applied inside `categorize_line`
*before* `determine_category` sees the score — `thresh_trash` and the six `check_rescues()`
call sites tied to those rules are gone from `determine_category`; `check_rescues()` is now
reached only from the QS-band routing at the bottom (`:1280`). Net effect: these six signals no
longer independently force `Trash` on their own — they only lower `qs`, and the *existing*
QS-band threshold (now fed a lower score) plus the PR's own rescue floors (reference floor,
forgiven-headline) decide the final outcome. This is not a full revert to the pre-June-15
cascade (the six penalties now interact with PR #32's rescue floors, which didn't exist in June),
but it is the same *style* of fix (cumulative subtraction) that the June pass explicitly
retired, applied on top of the newer design. Worth a deliberate decision, not just accepting
the fixture re-baseline as confirmation it's fine.

### 3 & 4 — Two ad-hoc patches
Both are single-purpose string/character checks rather than general rules, which cuts against
config-driven, allowlist-style detection used everywhere else in this file:
- **#3** (zero-alpha override) is narrow and low-risk — it only fires on lines with no letters
  at all, and only when every existing rescue (forgiven, reference, upright-Czech) has already
  declined to apply.
- **#4** (em-dash + digit) is broader than its one motivating case. It directly reverses the
  PR's own Testing-section argument that `"Výzkumná zpráva č. 1/2024 — Hradiště u Horní Mezí"`
  scoring `Clear` was a fixture *mislabel*, not a regression — the PR asked for an explicit
  choice between fixing the fixture or narrowing the change, and this is the "narrow" choice,
  but implemented as a literal substring+digit match rather than a rule scoped to what actually
  made that line problematic. It will also catch any other clean, correctly-read headline that
  happens to contain an em dash and a stray digit anywhere in the line.

### 5 — The actual answer to #30
This is the one change in the batch that is a general, principled fix rather than a special
case, and it is the literal content of this issue: the offline replay tool was silently scoring
lines on different signals (`text_content`, a single-tier lang trust) than production
(`original_text`, `classify_TEXT.py`'s two-tier trust). Recommend calling this out explicitly in
the issue thread and closing #30 against it once reviewed — independent of whatever is decided
about items 1–4 and 6.

### 6 — Dead code
Not a behavior risk, but `text_util.py` is currently 2136 lines, of which ~450 (21%) are inert
commented-out `determine_category`/`categorize_line` bodies (`:1383–1802`, three stacked
versions). Strip before `main`; keep whichever single version reflects the reviewed decision on
item 2.

### 7 — Fixture re-baseline confirms zero net `categ` drift
Cross-checked all three touched CSVs directly: every stored `categ` label is unchanged after
`b50c1f3`; only `quality_score` numbers moved (as expected, since item 2 changes how `qs` is
computed before the same QS-band thresholds apply). The parity tests passing is therefore a
meaningful signal here, not just a fixture rewrite to match new output.

---

## Verification (run against a fresh clone of `test` HEAD, `b50c1f3`)
- `pytest -m "not slow"` → **489 passed, 4 skipped, 4 deselected, 0 failed.**
- The four originally-failing behavioral tests
  (`test_short_gibberish_token_routed_to_trash`, `test_real_short_garbage_is_trash_per_line`,
  `test_inverted_trash_stays_trash_at_default`, `test_garbage_and_mirror_is_trash_or_nontext`)
  all pass, unchanged in their assertions (only a docstring comment was reworded).
- All four parity tests in `tests/test_recategorize_parity.py` pass; that file itself was
  substantially rewritten/expanded in the same follow-up (part of issue #5's unified re-scorer
  work) — worth noting it now covers materially more than the 4 tests the PR described.
- `_is_neutral_token`, `count_damaged_tokens`, `is_clean_reference`, the `2→z` lookbehind fix,
  and the CodeQL character-range fix are all present and byte-for-byte unchanged from the PR.

## Recommended next steps
1. Decide on item 2 (penalty cascade) explicitly — confirm it's intentional, or restore the
   independent-override style PR #32 shipped with, before this goes anywhere near `main`.
2. Replace item 4's em-dash+digit patch with a scoped rule (or accept the PR's original
   fixture-fix suggestion) — resolve the disagreement with the PR's own argument rather than
   leaving both "this is a mislabel" and "force it to Noisy" in the repo at once.
3. Call out item 5 in the #30 thread explicitly and close the issue against it once reviewed —
   it's done and it's the actual ask.
4. Strip the dead code (item 6) once item 2 is settled.
5. Only then: merge `test` → `main` and re-baseline `data_samples/` collection-wide, following
   the same pattern as #3's `v0.20.1` ship.

## Out of scope (other threads)
- The recall/precision trade-off PR #32 raised (72%→46% recall vs 31%→50% precision) is a
  policy call for #3, not #30 — flagged there, not resettled here.
- Parameter-importance ranking / ablation of the reinstated penalty rules — **#5**, if item 2
  is kept.
